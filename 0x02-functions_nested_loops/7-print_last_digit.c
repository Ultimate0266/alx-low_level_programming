#include "main.h"

/**
 * print_last_digit - print last digit of a number
 *
 * @n: is an integer
 * Return: last digit
 */
int print_last_digit(int)
{
	n = n % 10;

	if (n < 10)
	{
		n = n * -1;
	}
	_putchar('0' + c);
	return (c);
}
