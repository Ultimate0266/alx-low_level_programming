#include "main.h"

/**
 * rev_string - function that reverse a string
 *
 * @str: pointer string  is reversed
 * Return: nothing
 */

void rev_string(char *s)
{
	int i = 0;

	while (str[i])
		i--;
	_putchar(str[i]);
}
